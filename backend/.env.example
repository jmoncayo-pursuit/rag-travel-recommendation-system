# Backend .env.example

# Database connection details
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=travel_recommendation_db

# Server Port (optional - defaults to 3001 if not set)
PORT=3001